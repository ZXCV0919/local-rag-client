# SiliconFlow 与隐私边界

## 分工

选择 SiliconFlow 作为聊天提供方时，**只有生成阶段**会请求云端；文档嵌入与向量检索仍在本地完成。

## API Key

SiliconFlow 需要配置 API Key。当前版本密钥保存在本地设置中，请勿把含密钥的数据库文件发给他人。

## 适用场景

当本地小模型回答质量不足，又希望知识库数据不出境时，可采用「本地嵌入 + 云端生成」组合。
